import { createApp } from 'vue'
import App from './App.vue'
import {Quote} from "../build/types/phrase_typedef";

createApp(App).mount('#app')

// define themes and nuclei


// load json and generate Quote objects, put them in
//  a dictionary keyed by id. Then append ids to the correct
//  nuclei lists


let q = new Quote()

// sort the nuclei lists by speaker (immigrant parents first)



// construct quote content by id (Quote template dynamically loads its audio and video)
// define themes and nuclei
// controller is here, TreeDisplay reacts to global settings here